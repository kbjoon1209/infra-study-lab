services:
  app:
    image: tomcat:10.1-jdk17-temurin

    volumes:
      - ./app/ROOT:/usr/local/tomcat/webapps/ROOT:ro

  web:
    image: nginx:alpine

    ports:
      - "8086:80"

    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

    depends_on:
      - app